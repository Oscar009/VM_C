#include <iostream>
#include <bits/stdc++.h>

#include "virtual_machine.h"

int main(int argc, char *argv[])
{

    virtual_machine(*(argv + 1), *(argv + 2), *(argv + 3), *(argv + 4), *(argv + 5), *(argv + 6), *(argv + 7), *(argv + 8));

    return 0;
}